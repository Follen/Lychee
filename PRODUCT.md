# Lychee 产品约定

## 产品与平台

Lychee 是 WoW 游戏内的通用搜索工具，为内置与第三方 Provider 提供统一的搜索入口和呈现。技能、插件设置、货币、声望只是用例，框架按数据与交互能力扩展，不以游戏业务类别限制接入。Platform：**World of Warcraft native AddOn（正式服、熊猫人之谜怀旧服、泰坦重铸服务器「时光」、燃烧的远征周年纪念版）**。

核心流程是呼出面板、输入搜索、选择结果并执行；空输入显示固定项与最近使用，方便重复操作。界面应紧凑、便于扫描，尽快让玩家回到游戏。

## 已确认的视觉方向

用户批准了 uTools 风格参考图的紧凑布局，并明确表示“但是这个 UI 很可以”。后续修订保留该布局，面板底色比参考图更黑，主色使用红色。

现有 `addon/Lychee/Media/lychee-logo.tga` 是品牌标志的权威来源，使用原始资源。技能图标来自游戏数据；参考图中的示意图标不替代实际技能图标。具体尺寸、颜色和组件规则见 DESIGN.md。

## 操作边界

- 战斗中不能呼出；进入战斗时自动关闭，脱战后保持关闭。
- 技能通过鼠标点击安全按钮施放；Enter 不模拟受保护的技能施放。
- 技能成功后关闭面板并记入最近使用；失败后允许重试。
- 搜索结果与最近使用采用列表行，标题保持单行；固定项使用紧凑入口。空间不足时裁切，必要时悬停查看完整名称和动作说明。
- Provider 决定数据、默认和次要动作、是否拖动及拖动内容；宿主决定匹配排序、呈现、选择反馈、安全校验和生命周期。搜索结果与最近使用消费同一份交互声明。
- 鼠标和键盘共用唯一当前项。首页固定项不加方形选中底，名称下方短红线标记当前项；最近使用沿用结果行样式。Esc 使用红色反馈且不显示 tooltip。

## 搜索等待与动作

- 当前查询已就绪的结果先显示；慢来源继续准备，不以全来源完成作为显示条件。等待、无结果、部分失败和超时分别表达。
- 旧结果只可用于短暂视觉过渡，输入变化后立即撤销旧结果的交互能力。不能把旧画面计作新查询首批结果，也不能在新结果到达后沿用旧的按下身份。
- 功能来源开关只控制参与搜索；关闭面板取消搜索工作，不停止 Provider 的独立后台功能。所有者撤销业务可用性才进入停用生命周期。
- 暴雪设置中的音量、开关、数值和选项使用各自设置条目及动作分支。参数化输入生成 Invocation，预览不修改设置，用户执行后才写入；不另造重复音量来源。
- 各页底部保留统一社交入口；中文与英文构建按现有配置提供相应链接或二维码，使用紧凑浮窗。关于页中文保留“谨献给爱人：荔枝小月亮”。

## 交付边界

自带功能随一个 Lychee 包交付。独立第三方通过 SDK 1.0.0 接入，维护自己的媒体和存档；Host 不知道其业务实现。插件加载、搜索参与、数据准备和页面显示是不同职责，见[架构](docs/ARCHITECTURE.md)。

参考图代表获批的布局方向；本次迁移的真实客户端外观、战斗行为和性能仍需在游戏内验证。
