# Lychee 领域与所有权

- **Provider**：向宿主提供稳定或动态结果、动作及自定义页面的扩展。用户启用偏好与 Provider 运行时可用性独立，所有成功注册的 Provider 默认开启。
- **注册实例**：某次 Provider 注册的身份；同名注销再注册是新实例，禁用/恢复仍是原实例。管理页写操作校验实例编号。
- **结果快照**：一次命中或恢复产生的具名展示记录；ResultSnapshot 生成展示字段，ProviderRuntime 管理来源凭据。会话、查询代次、来源版本和注册实例各有独立含义。
- **查询发布**：一次当前查询的结果与等待状态；ProviderRuntime 随候选交付进度，Query 保持合并与发布顺序，SearchSession 校验会话后通过同一入口交给 Palette。身份变更可单独失效旧动作，不重新绘制页面。
- **首页**：固定项与最近使用的恢复、失效、交互绑定和控件复用由 HomeView 管理；Palette 管页面显示及整体尺寸。普通隐藏保留控件与内容，完整关闭释放活动引用，重开重新恢复。
- **交互绑定**：可复用目标当前代表哪个结果，以及鼠标按下是否仍属于这次绑定。Frame 复用不等于操作身份复用。
- **目录账本**：子插件目录已经成功提交的值与身份集合。失败不提前记账；枚举规则与分批方式归各业务 adapter。
- **角色固定项**：角色存档中的稳定引用及占位信息，不保存动作回调。UserPreferences 统一恢复和修改约束，异常原文保留、活动展示与写入受阻。
- **页面挂载**：ViewHost 管自定义实例/资源的 create、Mount、Unmount、Dispose；Palette 管成功后的展示切换以及失败、取消时的恢复。

内部 module 不自动成为公开 SDK。SDK 的公共范围以声明、版本契约及文档为准；不恢复数字位置业务记录，不以减少默认来源、静默丢数据或强制 GC 换取内存指标。

业务数据库归所属子插件。Host 只保存框架偏好及有界稳定引用；Player 用自己的角色 SV 保存业务设置与成就，其他包无持久需求时不创建空 DB。SDK 1.0.0 / API 1.0.0 提供独立 Storage/Catalog/Resources；项目装配不成为公共协议。
