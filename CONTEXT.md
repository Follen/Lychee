# Lychee 领域与所有权

- **物理插件**：本分支的自带功能位于一个 `addon/Lychee` 包；第三方独立维护 TOC、代码、SV 与媒体，使用 SDK 1.0.0 接入。目录模块不是原生按需加载包。
- **Provider**：向宿主提供稳定或动态结果、动作及自定义页面的扩展。用户开关只控制搜索参与，不停止后台或阻止明确引用恢复；所有者通过 `SetAvailability` 控制业务可用性。适用当前客户端的内置 Provider 默认参与搜索，EUI/EX 还需检测到当前角色启用了对应核心插件；角色显式设置优先。scope 不匹配的来源默认关闭且不显示设置。
- **Action / Invocation**：Action 声明行为、参数与执行；Invocation 标识目标、动作版本与规范参数。搜索和只读准备不执行动作；执行前重新校验，确认成功后记入历史。
- **注册实例**：某次 Provider 注册的身份；同名注销再注册是新实例，禁用/恢复仍是原实例。管理页写操作校验实例编号。
- **结果快照**：一次命中或恢复产生的具名展示记录；ResultSnapshot 生成展示字段，ProviderRuntime 管理来源凭据。会话、查询代次、来源版本和注册实例各有独立含义。
- **交互绑定**：可复用目标当前代表哪个结果，以及鼠标按下是否仍属于这次绑定。Frame 复用不等于操作身份复用。
- **目录账本**：内置目录已经成功提交的值与身份集合。失败不提前记账；枚举规则与分批方式归各业务 adapter。
- **角色固定项**：角色存档中的稳定引用及显示回退，不保存动作回调。UserPreferences 管理持久化边界，SearchSession 按可见需求协调加载、准备与恢复；不能把占位行当可执行结果。异常原文保留、活动展示与写入受阻。
- **查询会话**：Host 选择实际搜索范围、按需调用已声明来源并管理截止与取消。可用结果渐进发布，等待状态不是全来源放行屏障。
- **准备**：Provider 决定准备哪些数据，Host 协调有界任务。打开面板不意味着拉起全部第三方，关闭也不意味着卸载已经加载的代码或 SV。
- **页面挂载**：ViewHost 管自定义实例/资源的 create、Mount、Unmount、Dispose；Palette 管成功后的展示切换以及失败、取消时的恢复。

内部 module 不自动成为公开 SDK。SDK 的公共范围以声明、版本契约及文档为准；不恢复数字位置业务记录，不以减少默认来源、静默丢数据或强制 GC 换取内存指标。
