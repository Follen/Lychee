# 初始化分配、组件库与稳定文字动效

基线 4cecca9。用户实测 reload 后重复开关读数 28.59→14.23 MB，另一次 25.08→14.21 MB；GC 后再次快速开关未见明显增长。证据支持启动/首次使用临时分配，不证明全部来自索引或动画。

实施前预算：固定 2689 条真实 Provider/索引测试记录，对照启动累计分配、回收保留、结果语义；禁止减少目录、改变排序或增加运行时强制 GC。组件使用静态定义和保留实例，无变化更新不得重新建树，1000 次无变化更新预算 <64 KiB，关闭无活动业务引用。所有 Frame 保留复用，不能视为可销毁临时对象。

动效成本：主面板仍只有一个有界活动任务与短时驱动，增加三个固定 Frame：空背景（七个原有圆角区域移入）、裁切视口和固定内容根，不新增媒体。背景与视口边界同步；内容根显式锚定窗口最终几何，防止顶部内容在外壳之外显现。根框体、所有文字与安全按钮的尺寸/锚点固定。每帧最多更新背景/视口尺寸和中心偏移、根透明度及三个内容层透明度；变化检查，零每帧表/闭包分配。打开 440 ms，关闭 320 ms；无活动时移除 OnUpdate，隐藏/战斗/减少动态效果完成清理。不会对整个控件树逐帧 SetScale。

组件库不更改 SDK 工厂顺序或安全动作归属。协议、持久实例所有权、卸载和复用示例独立补充。公共模块需实际用于现有表单/页面，不仅导出未使用的包装。

验证结果与实机限制在集成后补充。wowdoc 来源见 2026-09-12-ui-library-wowdoc.json，retail latest 8ea15b61e45c0ed4eba01439c90757f86eb78d34。离线逻辑、内存与轨迹测试不能代替 WoW 字体渲染、帧时间与肉眼观感验证。

## 最终验证

- `tests/check_contract.ps1` 完整通过，日志 `%TEMP%/lychee-library-full-check.log`；79 Lua 的 wowdoc validate valid=true；Lua/XML/TOC、生成清单与 diff 检查通过。
- 启动固定2689记录五轮对照：累计分配21891.88→13133.21 KiB，减少40%；索引重建8648.38→2828.60 KiB，减少67.3%；保留6554.63→6554.67 KiB。详见startup-memory记录，非客户端整插件MB口径。
- 新UI库功能增加固定代码/定义元数据，TOC保留预算由1282增加64至1346 KiB，CPU仍46ms上限、基本Frame2/事件3不变。最终TOC累计3231.6 KiB、保留1317.8 KiB、19ms；前基线同机保留1273.4 KiB，增加44.4 KiB。这一新增能力固定成本与初始化临时分配下降分开报告。
- UI Runtime 1000次相同Update累计0.00 KiB、无原生setter/新Frame；非法声明重复提交不创建Frame，构造部分失败停止重试创建；输入默认值恢复、受控文本原生变化、旧点击/回调重入、combat释放动画、重复Release与任务清理均覆盖。
- 完整生产页面集成：Alias保存、换对象、隐藏/重开100次0新增Frame；搜索、来源管理、设置、检查器、结果列表保留业务回归。SDK ComponentPanel真实注册、ViewHost挂载、状态切换、默认输入重挂载与缓存复用通过。
- 1000轮主窗口完整及反向开关累计179.69 KiB、0新增Frame/动画组、关闭无更新回调；30/120Hz、冷/热重复打开、反向速度、减少动态效果、战斗、裁切边界等检查通过。旧整窗缩放明确失败于新增固定根布局断言。
- 组件库归并公共显示/输入/交互生命周期，特殊搜索算法、安全按钮和原生列表池仍由其业务控制器管理；不声称实现了React完整VirtualDOM或所有页面逻辑重写。

本次新增Presence、Runtime并修改TOC，交付后完整退出并重启客户端。实机待验证：首次登录/空闲/团本与姓名板峰值帧时间、所有页面开关观感、不同UI缩放、裁切与字体像素、快速反向、点击和战斗关闭。当前环境未直接执行游戏内验证，不将离线数字当作游戏内占用下降幅度。
