# 全局架构收敛验收

范围：审计 1—7；按授权移除独立 Lychee Performance Test。基线 `4ac169809691df2d13a540def812321b688de346`，后测为本提交工作区。设计见 [职责与成本](../design/2026-09-12-global-refinement.md)。原始离线输出见同名目录。

## 正确性

- 原审计真实 Provider→Query→Palette 复现：按下 A 后同一行改绑 B，旧实现执行 B。新回归普通/附加/首页/安全目标均拒绝旧点击，正常点击/拖动继续通过；隐藏、关闭重开、同物理安全按钮复用和释放引用均覆盖。
- 原实现 create/Mount 失败隐藏了原页面；现测试首页/搜索内容、焦点、会话和记录保留。回调中关闭/导航取消旧提交，重入 OpenView 返回 PANEL_BUSY。
- 独立审查发现并修复两条连带路径：关闭自定义页/替换失败需恢复搜索安全覆盖层；替换时目录更新不是用户导航，使用独立导航代次判断，避免取消后空白。新测试均覆盖。
- 固定项恢复拒绝 65 条、1 MiB 单条、超总量、空洞/重复/坏类型/metatable/未知字段；原数据引用保留，不能覆盖写入，正常撤销对象身份与角色隔离保留。
- CatalogLedger 覆盖原子失败重试、16 条批次部分成功、删除优先和取消/注册隔离；保留坐骑、技能各客户端业务与单项/原子刷新。
- ResultSnapshot 静态/动态/恢复等价、一次来源标记和独立身份有效性通过；管理状态与旧实例/回调重入通过，UI 不再持有内部注册记录。
- SDK 14 项故障注入与真实 Lua helper 验证通过；完整 22 个 SDK 文件受门禁约束。

## 性能口径

Windows 本机 Lua 5.1 离线替身；无真实游戏帧率或引擎内存结论。既有预算未调整。

| 固定场景 | 前 | 后 | 判断 |
| --- | ---: | ---: | --- |
| 2689 条目录回收后保留 KiB | 6873.6 | 6897.6 | 增加 24.0，低于 7168 门禁 |
| 48 次查询累计分配 KiB | 1782.6 | 1782.6 | 不变 |
| 重复查询保留增长 KiB | 0.1 | 0.1 | 不变 |
| 1000 Provider 管理页行数 / 替身数 | 8 / 356 | 8 / 356 | 不增长 |
| 管理页初次保留 KiB | 505.5 | 753.5 | 轻量展示字段占用增加，低于 1 MiB |
| 20 次管理刷新分配 KiB | 37.4 | 0.4 | 复用管理展示记录 |
| 固定项温读取 10000 次 KiB | 0.00 | 0.00 | 无复制 |
| 最近使用 100 次输入循环分配 KiB | 5891.9 | 5798.2 | 降低 93.7 |
| 最近使用同序列保留增长 KiB | 31.4 | 31.4 | 不变，0 新 Frame |

目录专项中，100 次无变化刷新累计分配从 9172.6 增至 9233.9 KiB（约 61.3 KiB 额外任务状态），保留增长为 0。它是架构复用收益，不能宣称内存下降。1500 坐骑专项分配 3318.4→3318.5 KiB，单次离线时间 82→81 ms，不据此宣称加速。

### 耗时波动如实记录

并行开发期间，最近使用测试曾出现单循环最大 6 ms，原门槛为严格小于 5 ms；增长仍为 31.4 KiB。固定三轮串行交替对照中，旧实现第 1 轮也触发原预算失败，第 2/3 轮最大 4/3 ms；新实现三轮均通过，总时长 146/139/138 ms，输出舍入后的最大值 4/5/3 ms（5 的显示值由浮点值舍入，未修改原断言）。旧实现通过的两轮总时长 120/119 ms。样本数和噪声不足以证明整体提速，新增身份管理存在固定成本。

最终串行 `check_contract.ps1` 完整通过，其中最近使用三次被不同集成入口执行，100 次循环总时长 119/102/106 ms、最大均 3 ms。所有前后原始输出保留；不隐藏开发期间失败，不放宽门槛。

## 完成检查

- `powershell -NoProfile -File tests/check_contract.ps1`：PASS（含全部原门禁与新增五项 Lua 回归、SDK 交付检查）。
- `lua tests/navigation_binding.lua`：PASS，含审查追加的返回安全按钮及目录更新/用户导航区分。
- `luac -p`：153 个本体/SDK/测试 Lua 文件 PASS；Bindings.xml 解析 PASS。
- 客户端生成/装配、SDK 声明/交付、数据生成等价检查：包含于全套，PASS。
- `wowdoc validate --path package/Lychee --source wow-ui-source --product retail --ref latest`：83 Lua，valid=true，无诊断。
- `git diff --check`：PASS。

## 交付与剩余验证

运行时提交后仅同步 `package/Lychee` 到唯一正式服 Lychee 目录并逐文件核对哈希；独立性能插件按明确路径删除，旧插件保留临时 ZIP 以便恢复。历史报告/SavedVariables、本体离线性能测试和原有未跟踪 `package/Lychee.zip` 不删除。

本次新增四个运行时模块，必须重启 WoW 加载新 TOC。尚未执行游戏内冷/热开关、真实鼠标/拖动、安全动作/战斗、角色切换及全部自定义页面验证；离线通过和文件同步均不等于这些场景已通过。回滚使用新的 git revert，并按相同验证/提交/同步流程交付。
