# 五包重构后的实机验证

## 范围与身份

2026-09-13，Retail 12.1.0.69587 / Interface 120100 / zhCN。测试源提交 `115d55b72848f38504aeec1ce60a569fb1c79456`，五包全部加载、16 个 Provider 全部启用、SDK/API 均为 `1.0.0`。178 个交付文件的正式服哈希与该提交同步清单一致。EllesmereUI 与 ExwindTools.UnifiedPanel 均真实存在，查询实际命中。

使用已有 Lychee Dev Automation 按需任务；不安装独立性能插件，不执行技能、物品、召唤、装备或设置写入，不注销/禁用真实 Provider。测试前面板未创建。自然 GC 策略不变，仅在四个明确快照执行完整 GC，并单列其成本。所有数字为五包合计；截图采集覆盖部分开关循环，不能据此推断无采集时的帧率。

## 证据

完整证据位于账户 SavedVariables `Lychee Dev.lua` 的 `LycheeDevDB.exports.records[TICKET].payload.content`，envelope 为 `lychee.evidence.v1`，来源为 `automation_result`，内层为 `lychee.automation.result.v1`。CLI 已验证请求身份、长度及 checksum，并确认接收。

| Ticket | 内容 | 结果 |
| --- | --- | --- |
| LYCHEE-20260913-064153-0022 | 加载与初始内存 | 完整 |
| LYCHEE-20260913-064727-0023 | 矩阵 v1 | 探针前置条件不当：法师测骑士技能、泛词假定坐骑命中；从设置返回立即开详情，延后读取时已卸载，后续循环未运行 |
| LYCHEE-20260913-064956-0024 | 矩阵 v2 | 探针摘要错误：把本地化表当字符串；未完成后续场景 |
| LYCHEE-20260913-065158-0025 | 矩阵 v3 | 56.111 秒，130 个断言通过，完整执行 |
| LYCHEE-20260913-065300-0026 | 最近20条错误 | 返回34条存量中的20条，均早于本轮；内层有 `encode:binary_string` 不完整标记，不能宣称完整错误审计 |
| LYCHEE-20260913-071825-0031 | 最终提交矩阵 v4 | 56.198 秒，130 个断言通过，完整执行，已确认接收 |

原始报告、探针、校验脚本、接口证据和截图保留在本地 `analyze/runtime-equivalence-20260913/`；Dev 原始接收文件在 `%LOCALAPPDATA%/LycheeDev/automation/received/<Ticket>/`。失败轮次不删除，传输 succeeded 与业务断言通过分开解释。准备探针曾检查不存在的 Exwind 全局，该字段不能判断依赖缺失；v3 已按实际 ExwindTools 入口核实。

## 已验证行为

- 17组输入各查询两轮：地下城简称/尾王/老二、怪物/技能、LDT 前缀、团本首领/团本、当前法师技能、实际收藏坐骑、成就、宝库、纹章、钥匙、EUI/EX 设置和无结果。所有预期来源均实际命中；两轮显示文字、ID、图标、动作ID与顺序摘要一致。保存了结果与标量 payload，但摘要不是完整动作参数的前后版本对照。
- 中文名称“荔枝大米助手”“团本首领”正确。设置页显示已注册来源，实测池5行、3个可见分组、16个注册来源；不把可见池数量当作全部来源数量。
- 扭缠盘蛇详情：14技能组、8行控件、模型显示、重置图标存在；同名组展开后列表14→16，子项实际可见；描述包含数字高亮。滚轮改变缩放，停止拖动后无模型 OnUpdate，关闭释放 context/怪物/组/资源。
- 正常开关30次、中途反向重开30次，两批各30。输入框相对 header 偏移约8，header 相对面板偏移0；60次无几何失败，搜索行池8→8。首页池首次访问内容后8→14，属首次容量扩充，不能宣称所有原生对象零增长。
- 每批关闭后查询任务、查询timer、动画任务、视图资源与输入焦点均清理；16个 Provider 的 SDK 托管资源计数均为0。该计数不代表枚举了所有模块原生事件或第三方插件的后台工作。
- 真实 SendInput Alt+Space 打开和 Esc 关闭另行截图验证。未执行真实安全动作、战斗、角色切换、Shift点击聊天链接；模型拖动只测回调释放，不冒充真实拖拽验收。

## 实测成本

以下为初轮 v3；最终修复后的 v4 结果见下节，二者不混作同一次测量。

| 阶段（GC后） | 五包 KiB | MiB |
| --- | ---: | ---: |
| 首次开面板前 | 11030.891 | 10.772 |
| 搜索与设置/LDT访问后 | 12253.142 | 11.966 |
| 第一批30次开关后 | 11588.525 | 11.317 |
| 第二批30次开关后 | 11588.572 | 11.317 |

第二批增加0.046875 KiB（48字节）；本次温循环未观察到持续保留增长。首次访问与第一批之间的负差含旧垃圾回收，不能称负内存或优化收益。全局Lua堆与单包计数分开保留。没有暂停GC，因此没有测“累计临时分配”；自然GC前后堆差不能替代该指标。

首次 Show 同步调用3.978 ms；两批温 Show 最大1.611/2.235 ms，Hide 最大2.269/2.393 ms。这是方法同步段，不是完整动画CPU。多数普通结果首次被50ms轮询观察到在50–104ms，技能“同步毒液”约459–462ms；最终全来源收尾普遍305–508ms。首结果延迟与最终完成延迟不同，原生调用/异步批次最长连续CPU未在本轮插桩测量。

每个完整GC约123–139ms、全局插件内存刷新约51–58ms。这是诊断对整个客户端的干预，未计入 Show/Hide 方法耗时，不能归因为 Lychee 的独占CPU，也不能加入生产关闭逻辑。

## 最终版本复核

矩阵 v4 的源提交为 `70c817dab961d148205bc1b4d45b76d8783348e1`，请求 `req-lychee-matrix-0913-04`。CLI 验证完整接收61713字节，SHA256为 `1b37b7771a33de9d120036891d2e3b8ac229758ccd6759e4d356a3a4eb71feda`。该轮不连续采集截图；17组搜索各两轮、设置、LDT详情与60次开关重新执行，130个断言全部通过，两轮查询摘要一致，两批几何失败均为0，关闭后各项托管资源清理正常。

| 阶段（GC后，五包合计） | KiB | MiB |
| --- | ---: | ---: |
| 首次开面板前 | 11032.716 | 10.774 |
| 搜索与设置/LDT访问后 | 12253.037 | 11.966 |
| 第一批30次开关后 | 11590.233 | 11.319 |
| 第二批30次开关后 | 11590.280 | 11.319 |

后30次仍只增加48字节。首次 Show 同步调用5.456ms，两批温 Show 最大1.719/1.689ms，Hide 最大2.031/2.622ms；不能用这些数字表示动画全程帧耗时。多数普通查询的首结果观察延迟50–104ms，“同步毒液”410/463ms，LDT前缀408/410ms，EUI首次206ms、第二轮50ms。查询最终收尾约50–464ms。此次全局GC约120–135ms，刷新插件内存约52–56ms，仍单列为观察成本。

最终冷启动真实 Alt+Space 截图 `analyze/runtime-equivalence-20260913/keyboard-show-final-crop.png` 已确认“荔枝大米助手 · 小怪”完整单行显示，随后真实 Esc 关闭。178个交付文件与正式服哈希一致，同步根目录为 `D:\Game\World of Warcraft\_retail_\Interface\AddOns` 下的五个 Lychee 包。测试完成后删除本轮 Dev 按需任务，任务清单为空，已发送 reload 清除已加载探针；不更改用户保存设置或清除证据。

## 实机发现与修复

真实 Alt+Space 截图发现首页最近使用的“荔枝大米助手 · 小怪”仍拆行。搜索列表已有有界测宽和单行规则，首页却固定90宽，标题固定留112；修正仅覆盖搜索行，遗漏首页复用卡片。

`tests/ui/ui_library_integration.lua` 在修改前准确失败于中文标签单行断言；改为80–160测宽、单行与相邻标题锚定后，通过长/短文本及固定/最近布局复用。沿用现有控件，不加对象、动画或定时器。设计规范同步补齐。

首次修复提交 `37a8512` 后的实机截图仍提前出现省略号。首页与搜索行改用 GetUnboundedStringWidth 测完整文字，再施加80–160上限；两处回归加入“受限测宽”和“未裁切测宽”不同的替身，旧调用准确失败，新调用通过。对应同版本 API 文档398行明确返回未约束文字宽度，完整证据为本地 `GetUnboundedStringWidth.json`。但不能把普通测宽认定为唯一实机根因：后续稳定态两种读数均为95.25，列宽98、IsTruncated=false。

`LYCHEE-20260913-070452-0027`、`070630-0028`、`070856-0029`、`071148-0030`（后三者均补全同样的 `LYCHEE-20260913-` 前缀）记录了显示/宽度及真实按键路径。稳定态截图完整，但首次按键截图曾提前省略。因此额外覆盖隐藏→显示边界：在首页已有 OnShow 中对可见最近项重新核对宽度，不创建 timer/Frame；同值不设置。回归模拟隐藏时未就绪文字指标，旧 OnShow 准确失败、增加复核后通过。首次帧问题与稳定态不可混为一谈，最终截图单独验收。

早期执行完整 interaction 用例时，旧峰值门禁出现5.000ms失败，早于新增布局断言；原始日志保留，不放宽预算。布局回归移至已有独立 UI 集成用例，完整验证结果另记收尾。

最终修复后全量89/89通过，报告为 `analyze/runtime-equivalence-20260913/full-show-final.json`；`wowdoc validate` Host共43个Lua文件有效、无诊断（`wowdoc-show-final.json`）；`git diff --check`通过。前两次修复的全量报告也保留，不覆盖失败证据。

接口证据：`sourceId=wow-ui-source`、`product=retail`、`requestedRef=12.1.0`、`resolvedCommit=8ea15b61e45c0ed4eba01439c90757f86eb78d34`。`Interface/AddOns/Blizzard_APIDocumentationGenerated/SimpleFontStringAPIDocumentation.lua`：339行 `GetStringWidth` 返回 uiUnit；580行 `SetMaxLines(maxLines:number)`；590行 `SetNonSpaceWrap(wrap:bool)`；720行 `SetWordWrap(wrap:bool)`。测量接口同版本 `PerformanceDocumentation.lua` 94行 `UpdateAddOnMemoryUsage`；`Blizzard_AddOnList/AddonList.lua` 761–770行明确注明刷新昂贵且不可放 OnUpdate。完整摘录见本地对应JSON证据。

## 等价性边界

这轮证明当前版本的上述真实场景与约定行为一致，以及同版本温/首次结果一致，**不是重构前后完整实机A/B**。历史 `LYCHEE-PERF-1789203670` 使用不同载体/私有重放范围，即使build相同也不能与本轮计算收益百分比。此前离线等价门禁也不等于实机所有业务、视觉和安全动作完全等价。

未完成项：旧版本同角色同输入实机A/B；全体Provider全部动作、真实技能聊天链接、角色切换、战斗/taint、团本和高密姓名板、长时间驻留、原生模型/纹理内存。未验证不按通过处理。
