# Esc hover

Esc now keeps a transparent background in every button state. Hover and press brighten the accent text; leaving restores the original accent. Existing event handlers and color guards handle the change, with no new callbacks, timers, frames, or idle work.

WoW source evidence: `wow-ui-source`, product `retail`, requestedRef `latest`, resolvedCommit `8ea15b61e45c0ed4eba01439c90757f86eb78d34`. The existing query in `docs/architecture/2026-09-10-result-labels-wowdoc.json` identifies `Interface/AddOns/Blizzard_APIDocumentationGenerated/SimpleFontStringAPIDocumentation.lua:675`, excerpt `Name = "SetTextColor"`; lines 682–685 define numeric RGB and optional alpha. The change reuses that setter through Theme and adds no new API usage.

Validation: existing contract/interaction suites, Lua parse, XML/TOC checks, wowdoc validation and diff whitespace check. Live visual confirmation requires `/reload`, hover over Esc, then move away. In-game visual and CPU measurements were not available for this change.
