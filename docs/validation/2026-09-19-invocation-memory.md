# Invocation 记录内存优化

实机归因 Ticket LYCHEE-20260919-183804-0007，正式服 12.1.0 zhCN，灵止光，已接收。764 条暴雪设置，Provider 记录树 15206 张表（仅结构计数，不是字节归因）；成就根仅 14 张表；索引根在 100000 槽位截断，不能报告完整大小。原始报告位于本机 LycheeDev/automation/received 同名 Ticket 的 report.json。

RecordCodec 在边界校验完成后，将单条记录中完全相同的 version/key 目标合并；空参数和单布尔参数使用已有 Provider 私有、弱值、最多 128 键元数据池。最多比较 16 个动作，无新全局索引、后台驱动或 SV；嵌套不相等目标不合并。公开记录和执行参数仍深拷贝隔离。SDK 1.0.0 格式不变。

固定 500 布尔设置样本回收后新增保留量：优化前 3752.76 KiB，目标共享后 3383.46 KiB，参数共享后 3325.00 KiB；减少 427.76 KiB（11.4%）。每条记录独立目标由 5 份降到 1 份。数据、动作与输入规模相同，不通过删功能降低占用。tests/providers/settings_memory.lua 验证目标复用和外部修改隔离；完整契约、Lua 语法、发布包通过。

wowdoc 隔离诊断副本扫描 111 Lua，仍为已记录的 28 条事件识别误报，valid=false；本次无新游戏 API。工具局限见 2026-09-19-wowdoc-review.md。实机整体收益另记录，固定样本结果不能当实机总量。
