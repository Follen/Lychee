# 加强主面板退场

用户反馈 684b422 的动画仍偏弱，尤其关闭。上一版 4% 收拢和系数 40 的 180 ms 阻尼轨迹，在 140 ms 时几乎不可见，退场大部分时间缺少清楚的空间变化。新加的中段可见性测试在旧实现失败。

本轮保持相同加载、输入、安全动作和任务生命周期，只调整运动轨迹：

- 整体尺寸变化 8%（92%–100%）。打开保留 300 ms，阻尼系数改为 26；约前 100 ms 完成显现。
- 关闭 280 ms，三次 Hermite 插值保留起始位置和速度，终点速度为零。完整打开后关闭，在 140 ms 时完成一半收拢，尺寸为基准的 96%，透明度约 0.802；最后 1 ms 的透明度已低于 0.0001，随后隐藏不会产生明显数值跳变。
- 透明度共用归一化位置/0.70 的 smoothstep，起止斜率为零；反向不更换映射，不重置位置或速度。顶边锚点和实际点击区域沿用上一版同步规则。
- 不新增 Frame、动画组、事件、任务状态或计时器。单个短时更新的关闭上限由 180 ms 改为 280 ms，是根据用户明确要求增强动效而调整的视觉时长；输入及安全动作仍在关闭开始立即失效。

预算保持：1000 轮（120 Hz 完整开关并含反向）累计分配低于 512 KiB、暖循环零框体/动画组增长、结束无活动更新。针对性测试本轮 179.69 KiB、总 CPU 51 ms、对象增长 0；时间仅为本机 Lua 替身测量，不是原生布局或游戏帧率。

新增/保留验证：关闭中段可见性、半程尺寸、末端连续消隐、打开/关闭的 30 与 120 Hz 一致性、快速反向速度继承、重复关闭、减少动态效果、战斗中断、布局重入与失焦 Esc。完整验证入口为 tests/check_contract.ps1，日志 `%TEMP%/lychee-palette-dismissal-check.log`。

无新 API。wowdoc source check 确认 retail latest 仍为 `8ea15b61e45c0ed4eba01439c90757f86eb78d34`；SetScale、SetPoint、SetScript 证据沿用 [上一轮接口记录](2026-09-12-palette-spring-wowdoc.json)。DESIGN.md 已同步实际参数。

尚未在真实客户端录制、观察或采样。交付后 `/reload`，重点比较关闭的收拢幅度、持续时间、末段消隐，以及低帧率和不同 UI 缩放下的表现。离线轨迹正确不等于实机观感已经获用户认可。
