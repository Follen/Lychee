# 主窗口共享底栏

基线961924443fc0de6e438d54b012783250edc65ce2。用户确认设置页“更改即时生效”到底边的距离正确，要求其他页面同类文字采用相同留白，并让三个按语言build切换的联系图标常驻。

## 布局与行为

- 首页、搜索（等待/空结果/有结果）、Provider详情、设置及设置详情统一使用56高底栏；文字中心距底28。18方形图标、28点击区、8间隔保持，点击区上下留14。
- 640宽窗口左右内距28；左状态宽144，快捷提示宽304，中间16间隔；两者并存时占据28–172、188–492。图标区512–612，与文字相隔20。只有状态或详情提示时独占464宽文字区。文字保持11号、单行裁切，不跨到图标区。
- 内容尺寸不减少：八行结果仍为410高，总窗口上限由518改为542，以容纳增加的24底栏高度；详情内容392时总高504。关于页原有56底栏，因此其362/310总高保持。小视口继续整体缩放，不单独缩小图标或文字。
- SocialLinks由Palette首次打开时创建并复用，父级为footer；随主窗口缩放、移动和透明度变化。设置页不再创建或隐藏社交栏。中文仍是微信赞赏/GitHub/作者微信；英文仍是PayPal/GitHub/X。没有改链接、文案或已确认可扫描的码图，没有恢复悬停tooltip。
- 浮窗和遮罩仍挂UIParent，保留原有紧凑尺寸。打开时清主搜索焦点，Enter/方向键和下一帧聚焦不能操作背后列表；Esc先关闭浮窗。普通关闭在首页/搜索恢复焦点，切页、详情、窗口退出和战斗关闭不抢焦点。主窗口隐藏时关闭弹窗；战斗下不主动隐藏受保护父级内的社交栏，依靠主窗口已有安全隐藏路径，脱战清理。

## 成本与验证边界

原先首次进入设置才创建的4个Frame（一个栏和三个按钮）移到首次打开主窗口；冷打开由50变54，首次进入设置后总池数量不增加。弹窗仍首次点击才创建，30轮中英文切换/点击不增加对象或元数据读取。不增加事件、timer、轮询、素材或持久缓存；底栏宽度只在单双字段布局变化时更新，预算沿用PERFORMANCE.md。

`tests/ui/about_social.lua`覆盖zhCN/enUS及zhTW/enGB回退、首次打开常驻、文字区域、完整八行、详情页、长状态、弹窗焦点、候选键、迟到聚焦、切页、复用和战斗。原有重开/滚动测试将原生视口替身从减88更新为减112，仍断言显示前高度到位、无重开高度动画和真实滚动边界。独立动画测试补充社交栏替身；设置性能测试在原测量区间之前创建实际共享栏，保持211个对象的装配范围及原门槛。

首轮全量100/103通过，失败为两处测试装配仍假定社交栏归设置页，以及重开测试写死旧高度。原输出保存在`analyze/shared-footer/tests-initial.json`，不是运行时回归已通过的证据。修正装配和尺寸预期后，三个对应测试单独通过。

最终`python tests/run.py --report analyze/shared-footer/tests.json`：103/103通过，含原性能门禁、静态语法、发布清单、中文输入法与重开回归。不含LDT加载1493.7490KiB/20ms，完整正式服1855.7939KiB/24ms，相对本轮基线常驻增加约2.59KiB，TOC阶段仍2Frame/4事件登记。最近使用100轮233ms总量、4ms峰值、0新增Frame；1000来源设置刷新仍为211对象、656.5KiB冷刷新保留和0池增长。文档及差异检查通过，未调整性能阈值。

独立布局评估与最终源码复核通过：字段边界不重叠，社交栏继承主窗口几何和透明度，导航/战斗/焦点路径有对应处理。Impeccable布局检测器前后均返回空结果，但该工具主要分析HTML/CSS，不能据此证明WoW原生渲染正确。

## 原生证据与交付

修改前wowdoc source check与inspect固定sourceId=wow-ui-source、product=retail、requestedRef=12.1.0、resolvedCommit=8ea15b61e45c0ed4eba01439c90757f86eb78d34。布局接口位于`Interface/AddOns/Blizzard_APIDocumentationGenerated/SimpleScriptRegionResizingAPIDocumentation.lua`，SetPoint第135行、SetHeight第124行；摘录包含`Name = "SetPoint"`及`IsProtectedFunction = true`，因此新的布局设置仍只在非战斗创建/显示阶段执行。`wowdoc validate`检查45个Lua文件，valid=true，无诊断。

游戏内字体裁切、不同UI缩放、图标随开关动画的实际合成与战斗taint尚未实测。离线检查不替代实机视觉验收。通过完整检查并提交后，按五包188文件同步及SHA-256校验，保留旧文件；具体提交和交付清单见`analyze/shared-footer/sync.json`。无新增模块或TOC，更新后执行`/reload`。
