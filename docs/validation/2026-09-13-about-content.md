# 关于页内容与构图重排

基线24f52eb9009a8ca77f8b84684e201e25213f1446。用户实机反馈：内容和展现空洞，与其他界面的完成度不一致。原页重复列产品分类和功能，再以“使用方式”占据主要内容，作者及献词退为脚注。

## 内容与构图

去掉操作教程和重复产品分类。左侧以产品名16号、“少一点翻找，多一点冒险。”14号定位及12号两行介绍构成阅读主体；介绍只概括已有搜索、固定和别名能力，不编造作者经历或开发故事。右侧独立显示作者标签、Follen署名和已安装版本，消除所有文字堆在左侧的构图。中文精确献词“谨献给爱人：荔枝小月亮”用14号柔和荔枝色作为下方收尾，英文不显示。

内容左列376，右列起点432、宽140；沿用黑底、已有字体和颜色，不加卡片、分隔线或重复Logo。关于内容高度由Theme统一为中文176、英文124，对应完整窗口362/310；底部社交图标、取消悬停tooltip及点击浮窗均保持已交付行为。

所有文字仍首次进入关于时创建、读取一次版本并复用；中文七个文字区域，英文六个，和改动前数量一致。没有新增Frame、纹理、事件、缓存或逐帧驱动。高度仍经Host既有ResizeForMode、非战斗保护和动效处理；预算不变。文案归Host词典，四语言启动使用真实Bootstrap初始化。

## 证据与验证

- 用户实机截图作为评审原始证据；独立审查先指出重复介绍、教程权重与作者/献词位置问题，再复核新版。
- analyze/about-content/preview.png：中文/英文完整窗口离线预览。内容、字号、颜色和尺寸由真实测试对象导出，字体采用微软雅黑近似；元数据9.8.7是测试值，顶部返回箭头是预览占位，生产返回控件未修改。预览用于内容关系与占位判断，不作为原生像素或实机合格证据。
- `python tests/run.py --report analyze/about-content/full.json`：102/102通过，含实际元数据、语言品牌、中文献词隔离、紧凑高度、切页/等待搜索底栏恢复、浮窗复用/关闭、全部静态及性能契约。
- 本次100次最近使用交互251ms总量、4ms峰值、无新增Frame；既有偶发峰值风险保留在上一轮布局验收记录，不据此宣称消除抖动。
- 不含LDT正式服加载1488.1660KiB/23ms，完整正式服1850.2109KiB/26ms，均为2Frame/4事件登记，沿用既有预算。
- wowdoc source list/check继续固定sourceId=wow-ui-source、product=retail、requestedRef=12.1.0、resolvedCommit=8ea15b61e45c0ed4eba01439c90757f86eb78d34；布局沿用此前确认的SimpleScriptRegionResizingAPIDocumentation.lua:135的SetPoint及:124的SetHeight，保持非战斗保护。未引入新原生接口。`wowdoc validate`检查45个Lua文件，valid=true，无诊断。

实机仍需确认STANDARD_TEXT_FONT字形、中文/英文换行、缩放和视觉接受度。离线测试只能证明内容与生命周期契约，不能替用户确认审美结果。

独立复核结论为ship（内容与离线构图范围）：教程式主体、重复介绍、作者字段化和献词脚注化四项均已解决；两列宽度与中英文字占位无重叠。该结论不扩大为原生客户端视觉验证。

## 交付

设计规范同步实际结构。检查通过并提交后，按五包188文件清单覆盖同步及SHA-256核验，保留旧文件；具体hash与清单写入analyze/about-content/sync.json。无模块/TOC/素材更新，游戏执行/reload。
