# 全量数据结构与内存审查（SDK 1.0.0）

## 范围与结论状态

基线提交 280be00cea3ff73b4e2aa0de486855140c4c16d8，单 Lychee 运行包。SDK / Provider API 固定 1.0.0。检查 Host、索引、SDK 边界、全部内置来源、UI 活动引用与存储；只修改有证据收益的结构，不为统一而重写已经紧凑的来源。当前离线通过，实机结果在下文补充；不能将此页视为全部客户端验收。

## 参考与取舍

重点阅读本机 EllesmereUI_GlobalSearch.lua：16–50 行为轻量文字和定位记录，385 行起为不创建真实控件的索引提取，搜索只服务设置导航。文件 SHA256 为 6FD33AF3DCB4BAB3A2D150E9F5E279E815B4D13820520BA4EA1C7E4F675C3918。上游位置：[EllesmereUI](https://github.com/EllesmereGaming/EllesmereUI)。采用轻量索引、结果阶段构造、固定行复用；不复制它的匹配规则，也不执行页面工厂来抓取数据。上游用途和 Lychee 全局业务搜索不同，不能直接比较主插件面板读数。

[SQLite FTS5](https://www.sqlite.org/fts5.html#external_content_and_contentless_tables) 将索引与外部内容分离，启发本次 SearchDocument / Entry 分离。[Elastic doc values](https://www.elastic.co/docs/reference/elasticsearch/mapping-reference/doc-values) 区分按词查记录与按记录读字段，说明没有必要把所有业务字段都塞进倒排结构。WoW 无法照搬 mmap/磁盘页缓存，SavedVariables 加载后仍占 Lua 堆；本次不把 SV 当作内存卸载。

## 所有权审计

| 来源/层 | 保留结构与决定 |
| --- | --- |
| Blizzard Settings（含音量） | 原来每项提前生成 1–5 个动作和嵌套目标。现在 Host 只索引搜索文档，Provider 持有原生设置定位；最终候选才生成完整 Entry。自然语言仍独立解析，默认/菜单 action 不变。 |
| 玩家技能 | 保留当前角色目录和共享动作；事件增量，不额外保存第二份 SV 全目录。 |
| 坐骑 | 保留已收藏目录，温查询不重新枚举；共享动作已降低每条成本。暂不为少量简单动作换成每次重新校验。 |
| 背包 | 动态事实按事件更新，保留安全物品动作。频繁变化，不适合落盘再维护差异副本。 |
| 成就 | 角色权威 ids/texts/categories，活动层复用原数据，只派生查找；不增加完整 Entry 副本。6002 项离线保留约 819 KiB。 |
| Boss / 团本技能 | 保留生成关系和 ID，名称与详情按需；不建全量技能名称常驻缓存。 |
| LDT | 保留有限生成事实，查询候选和当前详情才展开；模型/纹理单独统计。 |
| Ellesmere | 保留有界被动捕获的文字与定位；候选阶段创建 Entry，不建真实页面。 |
| Exwind | 查询作用域内读声明，Top K 才生成结果；结束清引用，不保留整布局复制。 |
| 游戏菜单 | 小型入口和共享动作，完整 Entry 成本低。 |
| 钥匙/小队 | 保留队友当前状态与事件职责；用户搜索开关不应关闭通信。 |
| 装备方案 / 天赋方案 | 小型角色目录，事件更新和稳定引用。 |
| 纹章 / 宝库 | 小目录，保持直接 Entry，避免增加调度与重复校验。 |
| 插件识别 | 活动取样有界，退出停止，保留复用控件。 |
| Host / SDK | 保留既有索引和排名；新增 documents 注册入口，弱登记完整候选身份。目录更新后旧动作失效。 |
| UI / 历史 | 有界可见行和稳定引用；不把弱登记误报为 UI 已释放，不把历史恢复状态充当完整目录缓存。 |

## 已实现

- 公共 RegisterProvider 支持 entryMode=documents 与 readEntry，默认 entries 不变；第三方和内置同样校验。Host Update 只保留 SearchDocument，不新增第二套 Catalog。
- reader 同步读取一个 ID；不执行业务、不扫描全库。命中读取失败标记不完整；更新、注销重入、错误 ID 与不匹配作用域均拒绝。明确恢复不依赖搜索可见性。
- 现有 CreateCatalog 修复被忽略的索引 checkpoint，暂停时间不占模糊评分预算；先取 K 个命中，缺失/重复再补查。一次性隔离记录移交调用方，省掉无意义的第二次完整深拷贝，不提供公开绕过校验能力。
- SDK 类型、协议、目录开发说明和性能规范同步，版本不变。

## 测量和被拒绝的方案

Lua 5.1 离线，同一机器与固定数据；不是游戏读数。

| 场景 | 基线 | 当前 |
| --- | ---: | ---: |
| 500 设置建目录后保留 | 3325.00 KiB | 1865.39 KiB |
| 综合来源新增保留 | 2381.1 KiB | 约 2128 KiB |
| 综合 100 次查询分配 | 4395.0 KiB | 约 7153 KiB |
| 综合 100 次查询 CPU | 37 ms | 73 ms |
| 2025 记录组合、48 查询分配 | 1900.8 KiB | 1900.8 KiB |

设置专项约减少 44% 常驻；动作生成移到候选阶段，每次仍需隔离校验，故查询分配增加。综合平均增加约 0.36ms/次。接受与否须看实机首批延迟及全部包保留，不仅看专项百分比。旧测试一次冷批次 5.83ms 超限，独立重跑通过，未调整 CPU 门槛。

拒绝过把设置接入独立动态 Catalog 的实现：同口径 100 查询约 17MiB 分配、约 221ms CPU，虽省常驻却明显增加复制与等待。最终保留原静态索引流水线，没有交付该接法。

29 组中文/英文词查询与基线源码逐字比较完整结果记录、动作参数、匹配证据：一致。新回归覆盖第三方 reader、修改隔离、更新失效、缺失/异常、重入、恢复、注销及索引 checkpoint。完整契约通过；设置中英文和音量即时刷新另通过。

wowdoc：wow-ui-source / retail / requestedRef 12.1.0 / resolvedCommit 4e3cbb8c5609e4bfc332c0aebbfa4d79731fab59，111 Lua、0 XML。仍有原有 event_not_found 28 条，不能报告 valid=true；见同日 wowdoc 审查。未新增游戏 API。验证工具是已记录的等价查询诊断构建，未修改上游源码。

## 实机验收

待本轮交付后，使用 lychee-dev 固定 26 查询 × 3 轮、全部来源统计，与 Ticket LYCHEE-20260919-184304-0008 的 16.138MiB 稳定保留比较。功能覆盖与性能分开，客户端战斗和英文实机不冒充已覆盖。
