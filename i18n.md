# Lychee 国际化与命名规则

本文件是界面命名、本地化与搜索语言行为的现行规则。修改用户可见文字、Provider 名称、别名或游戏名称解析前阅读；性能额度只在 [PERFORMANCE.md](PERFORMANCE.md) 维护。

## 三类名称分开

| 类型 | 规则 | 示例 |
| --- | --- | --- |
| 稳定身份 | 不翻译、不随界面名变化；用于注册、设置、固定项、动作和恢复 | `builtin.ldt`、`builtin.bosses` |
| 用户可见名称 | 必须通过所属词典解析；管理页、来源筛选、结果类型、提示保持一致 | 中文“荔枝大米助手”“团本首领”；英文“LDT”“Raid bosses” |
| 搜索入口与别名 | 显式声明，独立于展示名；改名保留已有有效入口 | `ldt` 继续可搜索，不应因此把中文显示名写成 LDT |

通用 UI 文案归 `addon/Lychee/Locales/UI.enUS.lua` 与 Host `I.Locale`；Provider 自有文案归其 `Locales.lua`，通过 `I.ProviderLocales:Module(id)` 获取。不得为改一个显示名在多个页面硬编码同一字符串。技术 ID、NPC/技能 ID、官方缩写和用户自定义文字不要求翻译。

## 语言与回退

- Provider 词典以 `enUS` 为完整键集合，可提供 `enGB`、`zhCN`、`zhTW`。解析次序是精确语言 → 语言家族（中文 zhCN，其余 enUS）→ enUS。
- 当前 Host 中文界面采用简体中文，zhTW 使用 zhCN 回退；这不是完整繁体翻译。需要原生繁体时必须补齐 Host 与 Provider 两条路径，不只改一处语言判断。
- Provider 其他语言不能新增 enUS 未声明的键；键缺失是开发缺陷，不能依靠内置词典的“返回键本身”掩盖。资源发布后不可原地修改，需要替换资源表使缓存失效。
- 翻译需保持参数数量、顺序和格式类型；完整句子用 `L:Format`，不要拆分可变语序句子拼接。`%` 使用正确转义；保留 WoW 颜色、超链接、纹理等标记的完整性。
- 语言随客户端会话确定；运行时数据和持久缓存的身份必须包含实际语言，不跨语言复用解析结果。

## 游戏名称与资料

技能、物品、首领、副本等优先使用对应客户端的游戏 API 本地化结果。构建数据保存关系 ID 和必要的有限回退名称；不可把中文技能名称当作所有语言通用名称。

尚未加载、API 不可用、确定不存在要区分。暂未就绪可显示本地化类别加 ID，并在有界加载完成后刷新；不得永久负缓存，也不得把占位名称合并成同名技能。技能合并先验证所属首领和技能 ID，名称相同不代表业务身份相同。

不为了翻译预建页面、不建立无限名称缓存；仅保留当前查询/视图所需结果，退出取消任务和迟到回调。性能、缓存和格式容量约束引用 PERFORMANCE.md，不在本文件复制数值。

## 搜索与显示

- 中文、英文、既有别名和前缀按产品契约匹配；展示名更改不能悄悄缩小搜索覆盖。
- 难度词是筛选条件：普通/英雄/史诗及明确的英文等价词，不改写技能原名中的同名片段；必须定义分词边界并测试歧义。
- 搜索结果说明保留具体归属和难度。来源名称用词典，游戏实体用游戏本地化，用户输入保留原文。
- 较长翻译要验证管理行、来源标签、按钮、提示和错误状态，允许合理截断/完整 tooltip；不得挤压主标题或把技术错误码当默认用户文案。

## 验收与文档同步

涉及本地化必须覆盖 zhCN/enUS，验证 zhTW/enGB 家族回退、缺键、格式参数、未知游戏名称、异步加载和源名称一致性。搜索改名还需验证旧前缀、角色显式关闭、固定项身份、结果动作不变。

当前测试入口：`tests/core/provider_locales.lua`、`tests/core/provider_locale_ownership.lua`、`tests/ui/i18n_ui.lua`，再运行变更 Provider 的业务测试与完整契约。SDK 公共语义变化同步其协议、示例和测试；仅内置名称变化不提升 SDK 版本。跨文件规则引用本文件，历史记录不覆盖现行规则。
