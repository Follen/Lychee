---
name: Lychee
description: 魔兽世界万用启动器
colors:
  window: "rgb(5.5% 5.5% 6.3%)"
  surfaceHover: "rgb(9% 9% 9%)"
  surfaceSelected: "rgb(8.5% 8.5% 8.5%)"
  accent: "rgb(83.5% 23.5% 28.5%)"
  accentMuted: "rgb(44% 15.5% 18.5%)"
  border: "rgb(11% 11% 12.5%)"
  text: "rgb(94% 93.2% 91%)"
  textMuted: "rgb(71% 70.5% 69%)"
  textDim: "rgb(59% 58% 59%)"
typography:
  input:
    fontFamily: STANDARD_TEXT_FONT
    fontSize: "16px"
  title:
    fontFamily: STANDARD_TEXT_FONT
    fontSize: "14px"
rounded:
  panel: "10px"
spacing:
  tight: "6px"
  control: "10px"
  content: "16px"
  section: "24px"
components:
  palette:
    backgroundColor: "{colors.window}"
    rounded: "{rounded.panel}"
    width: "640px"
  result-row:
    backgroundColor: "{colors.window}"
    height: "58px"
    width: "608px"
  result-row-hover:
    backgroundColor: "{colors.surfaceHover}"
  result-row-selected:
    backgroundColor: "{colors.surfaceSelected}"
---

# Design System: Lychee

## Overview

以已获批的 uTools 紧凑搜索布局为方向：顶部搜索、中间内容、底部状态。用户保留布局，仅要求使用原始荔枝标志、更黑的面板和红色主色。整体服务于快速查找和执行操作。

本文按 `package/Lychee/UI/Theme.lua`、`Input.lua`、`Palette.lua`、`ResultList.lua` 的当前实现记录。获批参考图是布局示意，**不是真实客户端截图**；用户已提供旧版搜索面板与提示框实机截图，作为本次粗重字体、红框和分隔线问题的依据；更新后的字体、缩放、外观与性能仍待客户端验证。

## Colors

颜色源自 Theme 的归一化 RGB，前置令牌用等值百分比表示。近黑底色贯穿窗口、顶部、内容、底部和输入区域；最近使用与搜索结果的当前项统一使用轻微提亮的中性炭灰底（RGB 8.5%），上下各内收 2，不加边框或阴影，也不混入红色；左侧 2 × 22 短红线标记当前行，不再包围整行描边。红色仅承担指示与操作强调，不给整行背景染色。主文本偏暖白，描述、分类和状态降低亮度。

红色用于操作状态和品牌强调；输入框聚焦时保持与面板一体的底色，通过文字与搜索图标变化提示状态，不增加红色输入框外框。右侧类型标签统一使用 `textDim` 灰色、11 号无描边字体，避免 Provider 自定义颜色打破整体层级。

## Typography

所有主要文本区域在创建时使用客户端 `STANDARD_TEXT_FONT`，不加描边并清除继承的文字阴影。输入 16、结果/提示标题 14、说明 12、分类/状态 11；只修改 Lychee 自有区域，不修改共享字体对象。前置尺寸以 WoW UI 逻辑单位表达，实际显示随 UI 缩放变化。

结果标题、摘要和类型标签各占单行；长文本裁切，悬停提示完整标题与详情，避免文字挤压图标和右侧操作。类型标签优先显示 `kindTitle`，缺省时依次使用分类显示名、Provider 显示名和“内容”；搜索结果与提示框使用同一份类型文案。

## Layout

面板固定逻辑宽度，顶部高 56，底部高 28。高度按内容在 164–600 之间调整；小视口整体缩放并留出边缘空间，保持居中位置和紧凑密度。

顶部左侧为原始标志，中间为 40 高搜索区，右侧为红色 Esc 关闭按钮，无 tooltip。空输入时最近使用最多五项纵向排列。搜索结果复用最近使用的比例：行宽 592、行高 46、行距 6；列表左边距 12，图标距行左侧 8、尺寸 28，标题距行左侧 48、字号 12。两者均以左侧 2 × 22 竖红条表示当前项，鼠标悬停与键盘选中共用同一浅炭灰底，背景上下内收 2。单行标题垂直居中，有副标题的搜索结果在同样行高中容纳两行。

搜索结果使用单列列表，一次最多显示八行，行间隔 2。滚轮及方向键翻动内容时复用现有行。图标位于左侧，名称与摘要居中，分类和可用的次要操作位于右侧。底部显示状态与操作提示。

## Elevation & Depth

深度来自不透明近黑面板、细分隔线和状态底色。顶部与结果之间使用留白；底栏只保留两侧内缩 16 的低对比细线。标志与搜索区之间使用 18 高的短竖线。当前面板没有外发光或投影层。

## Shapes

外壳使用固定圆角；内容行与小操作按钮使用平直边缘。圆角由可复用纹理区域组成，面板变高时保持角部尺寸。搜索结果与最近使用均为 28 方形图标。

## Components

- **品牌标志**：顶部以 42 方形空间显示原始 `Media/lychee-logo.tga`，保持资源本身的图形与颜色。技能图标直接使用游戏资源。
- **游戏菜单图标**：34 个入口统一采用 IconPark 1.4.2 的原始 SVG 轮廓，48 单位画布、3 单位线宽，浅色主轮廓与局部荔枝红点缀。构建时生成透明 64 方形 TGA，按搜索列表与最近使用统一的 28 实际显示尺寸检查；配色不改变分类标签的灰色样式。
- **搜索框**：放大镜、占位文案和输入共享面板底色。输入支持方向键移动选择、Enter 提交普通操作；受保护技能需鼠标点击。Esc 关闭面板。
- **结果行**：默认与背景融合，唯一当前项使用左侧短红线和轻微提亮的炭灰底，底色上下各内收 2；鼠标进入另一项会移动当前选择，方向键操作同一个选择。有次要操作时，仅当前项显示按钮。自有悬停提示分为标题、弱化类型、说明和底部操作区；主操作与拖动提示均使用次要文字色，不显示匹配百分比等搜索诊断。
- **最近使用**：空输入显示成功使用过的通用入口；没有记录时显示引导文字。分组标题使用小号灰字，在滚动内容内部留出顶部 10、左侧 12。技能、面板和命令使用同一网格；无图标时显示中性四格图形。悬停与搜索列表共享动作说明，点击和拖动遵循同一份 Provider 声明。
- **状态变化**：数据即时更新，视觉过渡遵循下文动效规范，不使用界面常驻 `OnUpdate`。结果行复用，重复的颜色、文字与尺寸设置由状态比较跳过。
- **动作菜单**：搜索行、最近使用图标和安全技能覆盖层均可右键访问全部已声明动作。使用原生菜单生命周期与 Lychee 专属样式：不透明近黑底、细边框、四侧 6 内边距；操作行高 32、文字 12 号、左右内边距 12，悬停采用下述 Esc 式前景高亮，内容宽度 156–280。普通动作按声明执行；选择次要技能动作后先准备对应安全按钮，需再次真实点击才能施放。菜单关闭或结果变化后，旧菜单不能继续操作旧结果。
- **战斗与施法**：面板通过 `SecureHandlerStateTemplate` 的战斗状态驱动只执行隐藏，脱战不重开。技能使用 `SecureActionButtonTemplate`，`useOnKeyDown=false`；收到成功施法事件后关闭并记录最近使用，失败保留重试机会。

## 菜单与功能控件的悬停规范

用户于 2026-09-10 根据“功能来源 / 已固定”和“返回搜索”的实机截图确认：菜单、功能导航和文字操作控件统一采用 Esc 式高亮。此节是已确认的目标规范；现有控件仍有背景高亮实现，写入文档不代表运行时已经完成改造。

- **范围**：设置标签、返回搜索、功能入口、文字操作按钮及弹出动作菜单的操作项；同类新增控件遵循同一规则。
- **悬停与按下**：背景保持透明，透出所在容器；文字及单色操作图标使用 `accentHover` 亮红色，与 Esc 一致。不添加矩形底色、外框、发光或尺寸变化。彩色内容图标保持原色。
- **正常状态**：普通控件沿用正常文字颜色，Esc 保留 `accent` 红色；离开或取消按下时恢复正常状态。悬停不要求所有功能文字常态变红。
- **当前选中**：标签页的红色下划线持续表示当前页面；悬停另一标签仅高亮其前景，不移动下划线、不切换页面。选中态与悬停态分别维护。
- **禁用与生命周期**：禁用控件使用 `disabled`，不响应悬停高亮；隐藏、重绑或按钮外释放后清理临时状态。采用现有鼠标事件和变化检查，不增加轮询或 Lua 动画。

搜索结果行和最近使用的内容选择仍遵循各自规范；此规则针对菜单与功能操作控件，不将整条内容结果改成红色文字。实际改造后需检查正常、悬停、按下、选中、禁用及离开状态。

## Do's and Don'ts

- **Do** 保留获批的紧凑布局，使用当前近黑底与红色交互状态。
- **Do** 使用原始品牌标志和游戏提供的技能图标，保持单行标题与悬停详情。
- **Do** 在真实客户端验证缩放、长标题、搜索滚动、施法和战斗关闭，并采样 CPU、内存与帧时间。
- **Don't** 把参考图视为实际游戏截图，或宣称未完成的客户端外观与性能验证已经通过。
- **Don't** 为装饰增加常驻每帧回调，或把 Enter 改为模拟安全施法。

## 提示框

提示框使用一个延迟创建、重复复用的自有 Frame，宽 280，内边距 14，圆角 8，完全不透明。五个 FontString 分别承载标题、类型、说明、主操作、拖动；缺失内容不占行，换行后按实际文本高度布置。操作区存在时使用一条低对比细线隔开。

提示框始终挂在 UIParent，定位锚点引用悬停入口右上方并限制在屏幕内；不将入口设为父对象，避免继承最近使用 ScrollFrame 的裁切。不响应鼠标、不跟随鼠标、不创建 OnUpdate。搜索行、最近使用与次要安全动作共享此样式；离开、结果替换、视图隐藏、打开动作菜单或关闭窗口时隐藏并释放 owner。游戏全局 GameTooltip 不被改动。

## 长列表与设置细节

成就来源使用现有奖杯图标，具体结果使用成就原生图标。结果副标题显示完成状态／条件进度、点数与“Shift 点击贴到聊天框”；普通点击定位成就，Shift 点击和右键“贴到聊天框”只填入聊天输入框，不发送消息。支持“成就：关键词”和成就 ID 检索。可选来源默认关闭，在功能来源中开启；目录分批构建，完成进度在查询时读取。

全局尺寸由 `Theme.Metrics` 唯一维护，首页、搜索、设置禁止另写一套行尺寸。窗口宽 640、高度随内容变化，上限 518（56 顶栏 + 32 底栏 + 20 留白 + 410 列表区域）；屏幕不足时沿用整体缩放。410 对应 8 行 × 46 + 7 间隔 × 6。设置标签占用这一区域顶部 42，不能在窗口外追加高度；短搜索仍按实际条数收缩。

所有列表行宽 592、高 46、间隔 6；左侧内收 12，图标 28、距行左 8，标题距行左 48。设置页双行文字使用相同字号和节奏，移除逐行横线。滚动条锚定外层内容区右缘，使用独立 12 宽槽，禁止跟随内缩行容器再次内缩或与右侧操作重叠。设置页滚动条顶端与滚动视口齐平，底端距内容区底边 2。

功能开关统一使用 `Components:CreateToggle`：32 × 18 圆角底座、14 圆形浅色滑块、左右边距 2；关闭为中性灰，开启为品牌红，状态文字仍明确显示。`SetChecked(checked, instant)` 只改视觉；业务动作由宿主校验身份后执行。180 ms 原生 OUT 位移与底色渐变同步，快速反向从当前显示位置继续。初次绑定、滚动换绑即时落位；隐藏调用 `FinishMotion`，减少动态效果时即时完成。滑块与渐变底座为同级独立层，关闭时滑块保持可见。不新增常驻更新或每次点击分配动画组。

首页、搜索结果、功能来源和固定项共用自有滚动条：3 单位宽、24–48 单位长的红色滑块、无可见轨道、12 单位透明鼠标命中区，无上下步进按钮。内容不足一屏时隐藏；支持滚轮、轨道点击和拖拽，拖拽结束或视图隐藏后移除更新回调。列表右侧预留滑块空间，继续复用可见行。

“功能来源 / 已固定”“返回搜索”“管理”和固定项操作默认使用次要文字色，悬停及按下只把文字和箭头改为红色；当前标签离开鼠标后恢复主文字色，禁用操作保持灰色。“添加固定”入口移除，固定仍从条目右键菜单完成。首领来源使用浅色骷髅图标。搜索使用独立、居中绘制的放大镜，输入与占位文本显式垂直居中。

钥匙成绩悬浮框沿用上述表面，宽 416；角色名使用职业色，总分与单本分数使用游戏评分色。副本、成绩、分数分列，成绩明确限时或超时，25 行距，数字右对齐。当前八本加表头首次创建 27 个文字对象，后续复用；普通条目恢复 280 宽。结果图标使用对应副本纹理，未知时使用通用钥石。

队伍钥匙默认声明 `searchGlobal=false`、`searchPrefixes={}` 与 `searchKeywords={"key","分数","钥匙"}`，Host 在完整触发时展示全队。忽略英文大小写和两端空格；默认不因副本名、角色名或通用模糊匹配召回队伍。用户可在管理页额外开启普通搜索、添加或移除快捷入口。整季成绩表只用于悬浮详情，单本成绩变动仍须更新；最近使用和固定引用保留原有解析规则。

## 交互动效

采用接近 iOS 的克制、连续动效：窗口尺寸变化是主要空间线索，内容过渡与控件反馈保持轻量。无弹跳、无整窗缩放、无逐条排队入场，不移动技能安全按钮的实际点击位置。

| 场景 | 规范 |
| --- | --- |
| 窗口打开 | 160 ms，透明度 0.72 → 1，原生 OUT 缓动；立即接受输入 |
| 普通关闭 | 100 ms 退场，立即使会话与安全动作失效并禁用输入；战斗关闭立即停止 |
| 窗口伸缩 | 220 ms cubic ease-out，从当前实际高度改向新目标；不排历史任务 |
| 页面切换 | 140 ms，透明度 0.90 → 1；离开页面停止旧动效 |
| 结果更新 | 新身份的标题／副标题 100 ms 轻过渡；不反复淡出整页，旧结果仍即时失效 |
| 当前项 | 中性底与红条 100 ms 交叉过渡；同状态刷新不重播 |
| 操作反馈 | 按下时文字透明度趋向 0.70，释放／取消后恢复；按钮尺寸不变化 |
| 悬浮提示 | 首次显示 100 ms 轻过渡，离开立即隐藏并清理，重复悬浮复用对象 |

待查询时保留当前窗口高度，不先缩为空列表；隐藏“没有结果”提示，显示搜索中状态。结果确认后再改变尺寸。内容区裁切子内容，伸缩期间不让内容溢出面板；独立提示层不受裁切。

滚动视口参与伸缩时，必须在首次显示、视口尺寸变化及内容几何变化后同步原生滚动内容边界（UpdateScrollChildRect），不能只更新自绘滑块。无变化通知跳过；隐藏或战斗中保留待刷新状态，下次安全显示时完成。通过尺寸事件更新，不新增空闲轮询，也不以反复隐藏条目强制重绘。

统一使用 `Lychee.UI.Motion`：`Reveal(region, kind)`、`Selection(region, selected)`、`Alpha(region, target, duration, callback, initial)`、`Height(frame, target)`、`Cancel(region, settle)`、`StopAll(except)`。透明度由原生 AnimationGroup 驱动；尺寸仅一个临时 OnUpdate 驱动，零任务时移除回调并隐藏。每个目标一个可复用原生组，最多 96 组，超出时即时应用；不无限缓存内容身份。

设置页以“功能来源 / 已固定 / 综合设置”分组。“动态效果”放在综合设置中，行右侧使用公共圆角开关并显示“开启／关闭”，禁止用无边界状态文字充当隐藏操作入口，不得放在顶部标签栏。综合设置首次访问才创建，后续复用。减少模式立即应用状态并停止空间过渡；保留颜色和选择反馈。快速关闭重开清理旧退场，禁止迟到回调关闭新窗口。所有动效必须覆盖隐藏、重绑、反向、战斗中断和停止后零活动检查。游戏实机帧时间与观感须单独验证，不以离线替身通过代替。

底栏统一高 32，提示文字与左右快捷提示垂直居中，两侧距窗口边缘 28。顶部分隔线同样内收 28、高 1，使用 footerDivider 中性灰半透明色；不得与滚动条接成封闭边框。首页、搜索和设置共享同一底栏，提示保持 11 号次要文字色。

背包物品结果统一提示“左键使用 · 右键更多”。右键菜单同时提供定位与别名。使用遵循游戏原生物品规则；定位打开背包并以红色细描边标记一个匹配格位，约 3 秒后消失。关闭背包、物品移动或进入战斗立即清除；高亮不覆盖物品图标，也不修改背包自身的悬停样式。

背包定位兼容暴雪原生、ElvUI、NDui 与 EllesmereUIBags。禁止以设置全局物品名称搜索代替定位，避免搜索框与过滤状态分离。右键定位清除旧过滤，Ellesmere 回到所有物品并滚动到目标，结果只使用短时格位描边；不安装背包刷新 hook。

异步来源未完成时，同步空结果仍属于搜索中，不显示“没有找到结果”；已有同步结果可以显示。异步完成或超时必须更新状态。成就目录按完成通知恢复等待查询，若等待查询已超时，目录就绪时自动刷新当前会话，禁止为等目录持续轮询。Shift 左键成就分享保持原行为。

仅包含空格、制表或换行的输入与空输入一致，显示最近使用，回车和方向键也作用于最近使用。未指定类别且规范化为空的查询不调用来源；显式“成就：”等类别浏览保留。

成就目录按版本、产品和语言共享一份只读参照目录；最多保留最近使用的十个角色，每个角色只保存共享条目的连续引用范围、无法共享的差异条目和按 ID 的后续更新。共享以实际枚举的 ID、名称和分类一致为条件，不假设不同角色拥有相同可见范围；新角色首次仍需枚举，恢复时保持角色目录集合、文字与顺序。完成状态与进度按结果实时读取。公共目录最多 32768 条，公共与差异记录合计最多 65536 条、名称合计 4 MiB、引用编码最多 65536 个整数；超限优先淘汰最久未用的角色缓存，极端情况下不保留当前角色缓存但不能截断当前搜索结果。正常重载分批恢复当前角色的独立可变目录，避免事件污染共享内容；分类数量变化只重新读取变化分类，获得成就只更新目标与系列。缓存损坏、角色未命中、格式/版本变化或超过七天审核期才重建，事件队列过载回退一次重建。禁止条件进度事件触发全量刷新、空闲脱战重建或轮询等待目录；停用释放活动目录和索引，保留共享目录与角色差异。


## 插件识别

入口名称“插件识别”，副标题“指向界面，查看来自哪个插件”，搜索兼容“这是什么插件 / 框体 / wtf / inspect”。这是默认关闭的可选来源，在功能来源里启用后出现；无须安装 WTFisThisAddon 或 LycheeDev。

进入后收起搜索面板，信息窗跟随鼠标，指向新框体时动态识别，无 F 锁定步骤。默认位于指针右下方，间隔 20 逻辑像素；靠近边缘时左右／上下换边，边界留白 16。按住 Shift 冻结当前目标与窗口左上位置并展开详情，便于阅读、复制和查看上一级；松开收起并恢复跟随。展开超出视口时只做最小边界修正，不重新运行鼠标避障。复制文本期间保持冻结，返回信息后恢复相应 Shift 状态。Esc 一步退出，主搜索打开、关闭来源或进入战斗也立即结束；脱战不自动重开。

浮窗是轻量信息卡，独立于主搜索。逻辑宽 320，跟随摘要高 140；Shift 详情高 366，需要启用来源记录时高 402。摘要不显示复制／上一级按钮：顶部小字“插件识别”，下面依次为来源名称、判断依据、单行框体名，底部提示 Shift 和 Esc。内距 16，标题 14、正文 12、辅助 11；长名称单行裁切，详情来源和父级各最多三行，完整信息保留在复制文本中。

详情用一条内缩细分隔线与摘要隔开；基本信息一行，创建位置、父级关联用低亮度小标题分组。底部复制和上一级各 140 × 30、间隔 8、圆角 6，使用低对比中性灰底，hover 提亮文字和背景，pressed 强化反馈，不出现宽大的方形 hover 块。动作只在 Shift 展开时显示，复制状态隐藏上一级。复用 Components 可选圆角背景与 Theme 现有圆角纹理；仅此组件启用，不改变其它按钮。红色仅保留目标自有 1 像素轮廓，不给信息卡与操作染色。

跟随使用有效缩放换算和变化检查，鼠标静止时不重设锚点。移除四角跳转及独立“技术详情”按钮；Shift 本身控制详情。换目标直接替换内容，不反复播放淡入，不增加追赶鼠标的缓动延迟。进入时保留共享 Motion 轻淡入，操作按钮使用中性灰反馈。极小视口只能保证面板处于视口内，不承诺所有尺寸下同时避开全部界面。

“创建来源”只来自框体本身的插件创建位置。暴雪创建代码可能是插件调用的共享模板，不能据此断言整个面板是原生：动态读取父级创建位置，有第三方来源时显示“可能来自 · 根据父级来源”，没有足够证据时显示“归属未确定 / 创建位置来自暴雪代码”。直接第三方创建证据优先，父级和名称匹配只能作推测；不得写死插件缩写与归属索引，不得将父级插件描述为修改者。技术详情包含类型、尺寸、创建位置和有限父级关联，复制框给出可选择文本与“Ctrl+C 复制”提示，不声称已写入系统剪贴板。来源记录未开启时摘要直接提示；“启用来源记录并重载”必须由用户明确点击，检查配置项存在且写入成功后才重载，不自动修改配置。此客户端配置会保留，停止识别不反向修改它。

性能：仅活动模式维持一个 0.1 秒识别定时器；可见且运行期间的 OnUpdate 只读取鼠标／Shift 并更新位置，绝不解析来源或遍历父级，隐藏立即解绑；同目标不重复解析来源，变更时父级最多 16 层并检查约 1 ms 预算。创建位置不可用或受限时明确显示未确定；不遍历全局框体，不缓存全部见过的框体，不修改目标脚本或父级。退出释放当前目标、报告和轮廓锚点并取消任务/订阅；复用固定 UI 对象。真实客户端动画、框体层级、受限操作和全局来源记录成本仍需游戏内验证。


## 客户端与双语界面

### Provider 二级管理页

功能来源列表与搜索列表共用 46 高行、6 行距、28 图标、48 标题起点及外侧滚动槽。整行轻灰悬停，点击进入管理；右侧管理入口与启停开关各自独立。正常来源介绍功能，只通过入口访问的来源显示“仅通过快捷入口”及实际名称，异常／停用优先显示。

二级页采用两列配置表单，替代多层说明堆叠。保留窗口尺寸、顶栏和来源身份行；正文左列是普通搜索／搜索前缀／快捷关键词，右列是实际配置及操作。标签左起8，值列左起136，内容宽592；标题12号主文字，用法11号次级文字。普通搜索的说明与开关同排。前缀和关键词以中性灰底28高、4圆角词条展示全部已配置值，不用单一示例替代词表；词条是只读值，不伪装成可删除按钮，编辑操作固定右对齐，与开关共用距内容右侧8的边线；词条区预留68宽操作列，不与编辑按钮碰撞。未配置仅显示“+ 添加前缀／+ 添加关键词”，不额外放未设置、状态或输入示例标签。用法说明紧贴值列下方，最多两行。

每类最多8个词条，依据本地字体实际宽度加20内距测量，6间距、34换行步长，长词换行不越过右边界。首屏常态为完整配置、添加／编辑、用法及底部工具行。普通搜索起点60，前缀起点104，常规设置项步长76；版本工具行紧随末项，展开版本常规内容高326（词条换行与错误另增高度），不因冗余留白产生滚动。版本与兼容性／恢复默认同排跟随正文，不再把恢复默认孤立固定在窗口底部。内容超出时使用外侧红色滑块。

每次只编辑一类词表，编辑框在值列原位替换词条，32高；逗号输入提示与唯一的保存／取消在其下。Enter保存、编辑框Esc取消，未修改也可完成且不写配置。开关即时生效；保存只替换当前字段并读取最新已保存兄弟字段，避免覆盖编辑期间的开关更改。未保存返回不写入；失败保留输入并在编辑区内提示。其他编辑入口在编辑期间禁用。恢复默认直接应用；来源重绑、编辑重开都使旧按压失效。中性词条和输入底面遵循现有主题；导航／保存／取消不添加块状悬停。

页面和16个词条容器首次使用懒创建、以后固定复用；配置变化后才测量词宽，普通打字不重排词表，布局键包括两份完整配置和编辑／错误／信息展开状态。隐藏清焦点、停止拖动和页面动画，不添加轮询、timer或事件。关键词仅承诺显示搜索结果，查询上限与协议保持现状。

UI 只消费 ProviderPolicy 的组合配置接口。旧 revision 4/5 模式在策略边界映射到原有效行为，Index 仍只接收通用来源过滤；不在 UI、Index 或查询排序里增加具体 Provider 分支。

### 搜索习惯与自定义别名

- 相同搜索优先显示上次成功选择的有效条目，不改变固定项与最近使用的顺序。综合设置提供“清空记忆”。
- 可持久引用的搜索结果和最近使用条目，右键菜单提供“设置别名”；背包条目的“定位”保留在该菜单中，左键使用不变。
- 综合设置提供“自定义别名 → 管理别名”。编辑在现有设置容器内完成，沿用统一面板宽度、行高、列表边距与外侧滚动条槽；不再创建不同尺寸的弹框。显示原名称、输入框、明确的保存／取消按钮；Enter 保存、Escape 取消编辑。空别名保存即删除，管理列表也可直接删除。
- 一个条目一个用户别名，保存上限与错误明确提示；不覆盖 Provider 原名、不复制条目。中文英文均可直接命中，不提供拼音匹配。
- 普通文字的命中部分用主题 MatchColorCode（浅荔枝红）提示，英文忽略大小写，中文按原文匹配。只改变字色，不加底色、不改变字重和行尺寸。含原有职业色、品质色或其他富文本的字段保持原样；提示和分享使用原文。
- 管理页复用控件；隐藏取消焦点与旧操作身份。用户编辑期间不被搜索刷新替换页面。

- 中文客户端显示「荔枝启动器」，英文显示「Lychee Launcher」；只给荔枝／Lychee染荔枝红，描述为「魔兽世界万用启动器」／「Universal launcher for World of Warcraft」。
- Host 文案与 Provider 文案分开注册和维护。内置与第三方 Provider 使用同一独立 i18n 契约，避免同名语言 key 相互覆盖。
- 语言切换沿用统一行高、图标和滚动条边距，不能为英文另建一套比例。短操作按钮优先用简洁动词（如 Back），长内容用已有省略／提示机制；真实英文排版仍须客户端检查。
- 客户端不具备的功能不显示可操作入口。功能名不使用内部 Provider ID、TOC后缀或客户端代号；这些字段只用于开发协议与诊断。
